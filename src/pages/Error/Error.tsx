import { IonContent, IonTitle } from "@ionic/react";


const Error: React.FC= () => {
    return(
        <IonContent>
            <IonTitle>Error Not found</IonTitle>
        </IonContent>

    );
}

export default Error;